import SwiftUI

extension Strings {

    enum PasswordGeneratorApp {

        static let generatorTabTitle = LocalizedStringKey("Generator")
        static let configurationTabTitle = LocalizedStringKey("Settings")
    }
}

