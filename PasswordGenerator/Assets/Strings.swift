import SwiftUI

enum Strings {

    enum Generic {

        static let okay = LocalizedStringKey("Ok")
    }

    enum Error {

        static let title = LocalizedStringKey("Something went wrong")
    }
}
