import Foundation

extension MasterPasswordKeychain {

    enum Constants {

        static let service = "PasswordGenerator"
        static let passwordKey = "master_password"
        static let hasPasswordKey = "has_password"
    }
}
